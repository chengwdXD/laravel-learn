CREATE TABLE `db_web01`.`students` (
    `id` INT NOT NULL AUTO_INCREMENT,
    `name` INT NOT NULL,
    `mobile` INT NOT NULL,
    `test` INT NOT NULL,
    PRIMARY KEY (`id`)
) ENGINE = InnoDB;

var_char
INT
date
